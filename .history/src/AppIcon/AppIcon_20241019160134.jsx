import classes from './';

const AppIcon = () => {
  return (
    <div className={classes.AppIcon}>
      <img src="/public/img/Logo.svg" alt="AviasalesIcon" />
    </div>
  );
};

export default AppIcon;
