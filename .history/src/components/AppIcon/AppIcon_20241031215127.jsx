import classes from './AppIcon.module.scss';

const AppIcon = () => {
  return (
    <div className={classes.AppIcon}>
      <img src="/img/Logo.svg" alt="AviasalesIcon" />
    </div>
  );
};

export default AppIcon;
